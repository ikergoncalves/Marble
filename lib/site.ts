/**
 * Canonical site constants.
 *
 * `SITE_URL` is the production origin used to resolve absolute URLs for
 * metadata, Open Graph images, `robots.txt`, and `sitemap.xml`. It's a
 * placeholder until the real Vercel deployment exists — update it once the
 * project is connected to its production domain.
 */
export const SITE_URL = 'https://marble-swart.vercel.app';

/** Human-readable site name, reused across metadata. */
export const SITE_NAME = 'Marble';
